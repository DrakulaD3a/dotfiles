#!/bin/sh

vol="$(pulsemixer --get-volume)"

readarray -d ' ' -t act_vol<<<"$vol"

case 1 in
	$((act_vol >= 1)) ) icon="" ;;
	* ) icon="婢" && exit ;;
esac

[[ $(pulsemixer --get-mute) == 1 ]] && printf "婢 0%%" || printf "墳 %d%%" "$act_vol"

